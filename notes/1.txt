1. проблема кэширования (если название файла тоже и он в кэше, то браузер может 
    не подгрузить заново этот файл, даже если в нем были изменения) решается:
    в webpack.config.js, поле output - динамическое имя готового файла [name].[contenthash].js
2. плагин 'html-webpack-plugin' добавляет в сборку html-файл и подключает в него скрипты
3. Декомпозиция конфига webpack в папке /config/build
4. была ошибка webpack-cli: решение удалить package-lock.json, node_modules и запустить npm install
5. Асинхронные компоненты (lazy loading, code splitting ... ): 
    -- Асинхронные компоненты подгружаются только при их использовании
    -- реализуются в react с помощью {lazy, <Suspense></Suspense>} from 'react'
    -- в сборке /build получаются отдельными файлами